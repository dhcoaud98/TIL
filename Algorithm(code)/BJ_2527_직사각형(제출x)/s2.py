import sys
sys.stdin= open('input.txt')

for tc in range(4):
    ax, ay, bx, by, cx, cy, dx, dy = map(int, input().split())
    # 직사각형 a, 선분 b, 점 c, 없음 d
    result = 'd'
    # 점(4가지)

    # 선(4가지)

    # 면(4가지)
