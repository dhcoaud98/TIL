# 03.20

## SWEA 프로세서 연결하기

<br>

DFS에서 while문과 break를 잘 이용하기

특히, 방문기록을 남기거나 방향을 함께 이용하는 경우

```python
while :  # 방문 가능한지 여부 확인하기
	if not:  # 방문 가능하지 않다면
        break  # break문으로 빠져나가고, else문을 실행하지 않고 다음 방향으로

else :  # while문을 break문이 아닌 조건에 의해 빠져 나오면 else 문이 실행되는데
    while :  # 방문 기록 남기고
        pass
  		DFS()  # 함수 실행
    while : # 함수 실행에서 return을 만나면 위의 함수 실행 줄로 되돌아 오고, 함수 실행하지 않고 해당 줄의 while문을 실행한다. 이 문제에서는 바로 이전 방문 기록을 지우고, 방향을 바꾼다. 
    if:  # 혹시 네 방향이 모두 막혀있다면 다음 프로세서로 실행하기
    

```

